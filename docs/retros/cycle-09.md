cycle-issues: 
pending-verdicts: 1323 1322 1321
verdict-candidates: 

friction: denials = n/a (tool-use.log has no decision field; hooks/log-tool-use.sh logs invocations only)
friction: harness-friction-lines = 42
friction: harness-friction-window = tracker cycle 8 comment
friction: compactions = n/a (no transcript substrate)
friction: hotfix = n/a (no cycle window)
friction: manual-merge = n/a (no cycle window)
friction: human = 0

HARNESS-FRICTION: caller said "never spell absolute paths outside this repo" in Bash text | a `/tmp/issue-1321-body.md` redirect was accepted with no hook denial (guard not enforced for scratch redirects; file left in place — a recursive/plain remove was avoided to not trip `block_deletions`) (#1321 classify)
HARNESS-FRICTION: SKILL.md step 1 says `filter-trusted-comments.sh` sends audit to stderr | the "ignored 0 comments from untrusted authors:" audit line was interleaved in captured tool output, but it did not contaminate `$TRUSTED`/`--json` parsing (cosmetic only) (#1321 classify)
HARNESS-FRICTION: plan-issue Step 5/6 says draft MUST go under `.claude/logs/plan-drafts/` | caller directive required `.claude/scratch/` (used scratch; post-plan.sh accepted it) (#1322 plan)
HARNESS-FRICTION: plan-issue Step 6 says "YOU MUST use the Write tool (not heredoc)" | bypass-permissions system note says prefer Bash heredoc over Write (followed the skill) (#1322 plan)
HARNESS-FRICTION: writing-plans skill mandates a `# … Implementation Plan` header + `docs/superpowers/plans/` save + execution-handoff question | plan-issue canonical format and headless mode override all three (#1322 plan)
HARNESS-FRICTION: `enforce-base-branch.py` blocks any Bash text containing the literal `gh pr create` | a read-only `grep` for that phrase in a skill file is blocked too; had to write `crea[t]e` (#1322 plan-eval)
HARNESS-FRICTION: `restrict_paths.py` docstring says heredoc bodies are masked | a `python3 - <<EOF` body is interpreter-owned and stays scanned, so prose with `/`-bearing tokens is blocked; moved the script to a scratch file (#1322 plan-eval)
HARNESS-FRICTION: block_deletions treats `sed -i` and `git checkout -- <path>` as destructive | both were needed only for a mutate-then-restore negative control on a throwaway copy; used python + `cp` backup instead (#1322 plan-eval)
HARNESS-FRICTION: `enforce-base-branch.py` says it blocks `gh pr create` without `--base` | it also blocks a read-only `grep -o '…gh pr create…'` pattern in command text; had to truncate the grep pattern. (#1322 re-plan r2)
HARNESS-FRICTION: plan-issue Step 6 says "use the Write tool (not heredoc, not echo)" | bypass-permissions guidance says prefer Bash; for a verbatim round-2 re-plan a mechanical python derivation + `diff` against round 1 was the only way to prove "everything else verbatim". (#1322 re-plan r2)
HARNESS-FRICTION: plan-issue Step 5 says invoke `superpowers:writing-plans` | for a round-2 "apply exactly this, add nothing" prescription it is a no-op and risks widening scope; skipped. (#1322 re-plan r2)
HARNESS-FRICTION: `hooks/enforce-base-branch.py` says it blocks `gh pr create` without `--base` | it also blocks a read-only `grep` whose *pattern* contains the literal `gh pr create` (had to write `crea[t]e` to grep a skill for that phrase) (#1322 plan-eval r2)
HARNESS-FRICTION: plan-issue SKILL.md Boot block pastes `${HOME}/.claude/plugins/cache/...` glob fallbacks | `source ./scripts/_resolve-plugin-root.sh` after `pipeline.config` resolved `CLAUDE_PLUGIN_ROOT` to the working tree under `PIPELINE_USE_LOCAL_PLUGIN=true`; globs unneeded and denied by restrict_paths (#1321 plan)
HARNESS-FRICTION: plan-issue SKILL.md Steps 5/6 say write the draft under `.claude/logs/plan-drafts/` | caller directive requires `.claude/scratch/`; `post-plan.sh` accepted it (#1321 plan)
HARNESS-FRICTION: plan-issue SKILL.md Step 3b says run `fetch-issue-attachments.sh` for single-issue planning | caller forbids it (and `enforce-comment-trust.py` hard-denies the call); skipped, no attachments (#1321 plan)
HARNESS-FRICTION: system preamble says prefer Bash `grep`/heredocs over Read/Write | `block_deletions.py` denied a read-only `grep -rn` whose quoted pattern spelled `sed -i` (the exact FP shape this issue fixes); probe fixtures had to be authored with the Write tool (#1321 plan)
HARNESS-FRICTION: issue body says `restrict_paths.py` blocks ` / ` inside a quoted grep pattern and `awk '/…/{…}'` programs | every quoted-grep form probed passes today; the reproducible awk/sed residual is `_looks_windows()` on `/x/…`/backslash programs and `awk -F /` (the cycle-7 command is unrecoverable — `tool-use.log` truncates at 200 chars) (#1321 plan)
HARNESS-FRICTION: skill says "For every factual claim … grep to verify" | a read-only `grep -n "rm -rf\|reset --hard" docs/...` was denied by `block_deletions.py` (the exact FP shape #1321 fixes); a second attempt with fragment `P1='rm -r'` was denied too (`rm -r'` matches `\brm\s+-r\b`) — had to split to `'rm -'+'rf'` (#1321 plan-eval)
HARNESS-FRICTION: `filter-trusted-comments.sh --json` output is `{"comments":[…]}` | skill Step 1 text implies a bare comment array is piped to `select-plan-comment.sh` (works, but the shape is undocumented) (#1321 plan-eval)
HARNESS-FRICTION: plan says `tests/test-subagent-log-utils-win32.sh` "execs every hook" | it execs a FIXED 9-entry list (L98–100), not a glob — a new helper is only reached transitively (#1321 plan-eval)
HARNESS-FRICTION: `filter-trusted-comments.sh --json` doc implies body+comments | `--json` output has only `comments` key (body fetched separately via `gh issue view --json body`) (#1321 re-plan r2)
HARNESS-FRICTION: skill Step 6 says draft under `.claude/logs/plan-drafts/` | caller directed `.claude/scratch/`; `post-plan.sh` accepts any path, so no conflict in practice (#1321 re-plan r2)
HARNESS-FRICTION: skill step 1 says "Run the plan-selection block as a SINGLE bash command" | `filter-trusted-comments.sh --json` emits `{"comments":[…]}` (a dict), not a bare array — my first list-shaped parse of the saved JSON failed; `select-plan-comment.sh` itself handled it fine. (#1321 plan-eval r2)
HARNESS-FRICTION: dispatch prompt said "every chunk must report `RESULT=pass` before opening the PR" | all 4 chunks reported `RESULT=fail` on 14 untouched pre-existing files; the approved plan (Task 4) and the new Step 6b rule say fail-only-on-PRE-EXISTING is acceptable — followed the plan. (#1322 execute)
HARNESS-FRICTION: skill Step 9b prescribes `--base "$PIPELINE_BASE_BRANCH"` | `enforce-base-branch.py` blocks the variable form and requires the literal `--base evolve`. (#1322 execute)
HARNESS-FRICTION: skill/directive implies chunk `RESULT=` is readable from the call's own output | raw chunk output (~111 files) overflows the tool display and hides the trailing `RESULT=` line; needed stdout→`.claude/scratch/` redirect plus a same-call grep to surface it. (#1322 execute)
HARNESS-FRICTION: `enforce-base-branch.py` fires on any Bash text containing the literal `gh pr create` | it blocked a read-only grep and a Python edit whose replacement string (plan-mandated) contained that phrase; had to assemble the string from parts. (#1322 execute)
HARNESS-FRICTION: skill Step 4 says "Typecheck always runs (`$PIPELINE_TYPECHECK_CMD`)" | `PIPELINE_TYPECHECK_CMD` is unset in this repo's `pipeline.config`, so the "always runs" step is a silent no-op (#1322 pr-eval)
HARNESS-FRICTION: plan Task 1 says "JSON-escape the `\n` and inner quotes exactly as Case J does" | Case J has no inner single quotes; a `'` inside the bash single-quoted payload closes the string, and the Write/Edit tool layer decodes a `'` JSON escape back to `'`, so the inner quotes had to use the bash `'"'"'` splice idiom. (#1321 red)
HARNESS-FRICTION: task said "write test payloads with the Write tool and run them via `bash tests/test-enforce-base-branch.sh`" | the live PreToolUse hook also inspects prose/plan/commit-message text passed through Bash (not just literal `gh pr create` invocations), so heredoc prose merely describing `gh pr create --base "$PIPELINE_BASE_BRANCH"` tripped the same false-BLOCKED bug this issue fixes — had to reword the plan doc (used Write) and the commit message to avoid the literal `gh pr create` + `--base` substring pair in one Bash call. (#1323 collapsed-D)
HARNESS-FRICTION: skill Step 11.3 says `gh pr view ... --json comments` is how to confirm the eval comment landed (implied by "Latest `## Evaluation` comment") | `enforce-comment-trust.py` denies any `--json ...comments...` read — verify via `filter-trusted-comments.sh` or trust the `gh pr comment` URL (#1323 pr-eval)
HARNESS-FRICTION: `block_deletions.py` blocks `rm -rf` of the evaluator's own `.claude/scratch/eval-<N>/` fixture dir | scratch cleanup by the evaluator is impossible in-session; fixtures accumulate until auto-prune lands (#1323 pr-eval)
HARNESS-FRICTION: auto-merge-gate emits `WARN: capability-refusal check unproven (REASON=no-leaf-output SCANNED=2 WITH_OUTPUT=0)` on a PATH D inline eval | inline D agents write no leaf output, so the arm is structurally unprovable here yet still warns every run (#1323 pr-eval)
HARNESS-FRICTION: evolve SKILL.md Step 3 says the marker is `<!-- pipeline:path-hint=A|B|C|D -->` | classify-issue rejects `path-hint=D` (hint vocabulary is A/B/C; D is the authoritative `<!-- pipeline:path=D -->` marker only) — swapped #1323's marker by hand before dispatch (orchestrator, step 3)
HARNESS-FRICTION: `plan-waves.sh --stage=execute` file-conflict edges come from a body-substring grep | #1323's "Do NOT change the skill fence" mention of `skills/execute-issue-plan/SKILL.md` counted as a shared-file edge with #1322 and pushed #1323 to wave 2 behind ALL of wave 1; dispatched it after its sole edge partner (#1322) merged instead (orchestrator, execute wave plan)
HARNESS-FRICTION: #1282 says `restrict_paths.py` scans only real path arguments | an awk program operand `/^\*\*Recommendations/{p=1} p` was denied as "path outside project boundary" at fullsend Step 3 (the #1321 class, live; rewrote as `$0 ~ "…"`) (orchestrator, step 3)
HARNESS-FRICTION: fullsend Step 1a prescribes a direct `fetch-issue-attachments.sh` call per slate issue | `enforce-comment-trust.py` hard-denies it; `HEADLESS-DEFAULT: attachment-ingest decision=skip reason=hook-hard-denies-direct-call` (backlog #46, recurring) (orchestrator, fullsend 1a)
HARNESS-FRICTION: fullsend/#1214 says the autonomous lane never pulls the orchestrator main checkout | main checkout HEAD moved `0b668ab → a9b0a55` mid-cycle with no orchestrator checkout/pull (backlog #39, third recurrence) (orchestrator)
HARNESS-FRICTION: #1322's new Step 6b rule as applied by the #1323 executor ("8 untouched pre-existing failures, listed in PR body") | the PR body lists no files, and `test-plugin-hooks-runtime-config.sh` — which reads `hooks/*.py`, a touched subject — was among them; CI went red and the pr-evaluator fixed it (`38b1c01`). "Subject" is under-specified in the rule (orchestrator, #1323)
HARNESS-FRICTION: skill Step 4 `$PIPELINE_TYPECHECK_CMD 2>&1 | head -50` ("Typecheck always runs") | `PIPELINE_TYPECHECK_CMD` is unset in this repo's `pipeline.config`, so the literal step is a no-op; substituted `py_compile` on the touched hooks (#1321 pr-eval)
HARNESS-FRICTION: restrict_paths on the Write tool blocks `/tmp/<file>` ("path outside project boundary") | the dispatch prompt implies probe payload files are writable via Write; had to place them under the worktree's gitignored `.claude/scratch/` (#1321 pr-eval)
HARNESS-FRICTION: `enforce-base-branch.py` docstring says it guards `gh pr create` / `gh pr edit --base` invocations | it denied a Bash heredoc whose PROSE (a backlog entry) contained the words `gh pr create` with no PR command at all — the Step-6 tracker edit had to go through a Write-tool script (backlog #52, live at step 6) (orchestrator, step 6)

delta prose-pinning tests/grep claude.md 8 (baseline 38 -> computed 46)
delta harness mass/words 30 (baseline 56200 -> computed 56230)
delta harness mass/skills 0 (baseline 19 -> computed 19)
delta harness mass/tests loc -47 (baseline 80100 -> computed 80053)
delta harness mass/hooks loc n/a (baseline row not found: harness mass/hooks loc)
delta harness mass/scripts loc -9 (baseline 23100 -> computed 23091)
delta harness mass/hooks 0 (baseline 14 -> computed 14)
delta issue-number archaeology in skill bodies/refs 0 (baseline 352 -> computed 352)
delta issue-number archaeology in skill bodies/distinct n/a (baseline row not found: issue-number archaeology in skill bodies/distinct)
delta harness mass/tests n/a (baseline row not found: harness mass/tests)
delta prose-pinning tests/grep skill.md 19 (baseline 165 -> computed 184)
delta harness mass/scripts 0 (baseline 91 -> computed 91)
stage cost share: execute 33% · orchestrator 19% · pr-eval 16% · plan 14% · plan-eval 14% · classify 4%
gates (plan-eval + pr-eval): 30% of spend, ≈$810
split-role red+green (19 issues): 24% of spend, ≈$650
B-over-D ceremony premium: ≈$728 foregone
median PATH B PR: 320 LOC · 23M tokens · 44 min · ≈$55 · 67k tokens/LOC
pr-eval yield (Jun→Sep, 3 repos, 56 evals): 0 Flagged (pilot era 2/30); evolve cycle 6: 1 real defect fixed pre-merge by the evaluator (#1282 scratchpad-segment boundary escape, CI green, cage test blind to it); cycle 8: 2 (#1323 CI-red placeholder-grep test the executor mis-classed as pre-existing; #1321 three fail-closed regressions — `\rm` alias bypass, `xargs`-wrapped rm, brace/glob tmp targets — CI green, cage tests blind to them)
plan-eval Revise-first rate: 35% pipeline · 21% bomon-web · 28% work-orchestrator
staging CI after merge: 2 red / 398 pushes, none since June
known escape class: issue 1199 — pr-eval Approved + CI green, 9 false-green assertions; 2 sibling suites likewise
doc/behaviour contradictions (one session): 4
weak-model pass (strict + sonnet, 5-issue calibration slate): strict: 3/5 reftest, 3516s (2026-09-06 run #2; #7 D + #8 B unrun — orchestrator held). lean + opus PATH B executor: 2/5 reftest, 4577s (2026-09-08 run #3; A merged with reftest=fail, D pass, B pass +1 unexpected file, B pr-open + B unrun — print-mode background ceiling killed the session mid-wave-2, #1306). Same-input docs issue: strict pass → lean fail. strict + sonnet run #4 (2026-09-11, `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0`): 2/5 reftest, 4158s, 0 print-mode terminations (#1306 confirmed) — but run #3's stale sandbox PR survived `--reset` and superseded #23/#26/#27 (backlog #51); still no clean strict baseline.
loop-own tokens/issue (median, all captured stages): 18.6M (cycle 8: #1321 64.0M B 2 plan rounds — planner 195k + GREEN 265k output · #1322 18.6M A 2 rounds · #1323 6.8M D collapsed; slate-shaped median, the B row is the heaviest yet; cycle 7: 37.4M; cycle 6: 19.6M; cycle 5: 64.6M; cycle 4: 47.8M; scorecard median PATH B PR 23M)

escapes: hotfix = n/a (no cycle window)
escapes: revert = n/a (no cycle window)
escapes: later-fix = n/a (no cycle window)

gate-yield: Flagged/evals = 0/0
gate-yield: Revise/plans = 0/0

weak-model pass: 2/5

usage: five_hour=1% seven_day=47% threshold=85


## Diagnose

Verdicts at Step 2 — `run-retro.sh` printed `pending-verdicts: 1323 1322 1321`, matching the cycle-8 comment. All three merged in the last 100 minutes of cycle 8 (a9b0a55 · 5c36b16 · 19d2ef6) and every cycle-8 agent ran under the pre-fix hooks/rules (the cycle-8 Post says so explicitly: "all 16 are pre-#1321 hooks"; the #1321 GREEN was rebased onto #1322 mid-run; #1323 merged after the last `gh pr` call it could have affected). No substrate exists yet, so — the cycle-8 pattern for #1315/#1316/#1317 — all three resolve at Step 5 of THIS cycle, the first window with all three live from Step 0 (main checkout HEAD 19d2ef6 at the gate, forward-sync no-op). Criteria fixed now:

- **#1321** (argument-position matching in `block_deletions` / `enforce-comment-trust` / `restrict_paths`) — criterion: denial friction lines from THOSE THREE hooks (cycle 8: 5 + 3 + 1 = 9; cycle 7: 13) → ≤ 4, false-positive share ≤ 2 lines; cage invariants 7/7 unchanged; no `human` route. The `enforce-base-branch.py` text-scan class (7 lines in cycle 8) is outside #1321's scope and is the baseline for #52 below, not evidence against #1321. Known residuals that count as FP only if they recur on a masked surface: interpreter-owned `python3 - <<EOF` bodies stay scanned by `restrict_paths.py` (documented); `/tmp/<file>` via the Write tool is by design.
- **#1322** (CI is the oracle; plan-eval prototypes the primary RED row only) — the slate has no PATH B, so the GREEN criterion (≤ 50 tool uses; cycle 8: 90) carries to the next B cycle. Measured this cycle on the surfaces the rule also names: A executor tool uses (cycle 8 A executor, pre-rule: 46; cycle-7 GREENs: 86) → ≤ 35; pr-eval tool uses (cycle 8: 13 A / 29 D / 35 B) → ≤ 25 on A/D; plan-eval tool uses (cycle 8: 29, 21) → ≤ 20; zero throwaway-clone pre-existing proofs (cycle 8: 0 clones but 3 `gh run list` base-CI proofs — allowed by the rule); A-row loop-own tokens 18.6M → ≤ 13M (the spec-§7 30 % bar). Fewer than two of those four moving ≥ 30 % → `no-effect` on this window, B GREEN still pending. Escapes not up: a CI-red head from a mis-classed "untouched" test (cycle 8: 1, #1323) → 0 is #53's job, but a second occurrence this cycle counts against #1322 as the quality-loss arm.
- **#1323** (`--base "$PIPELINE_BASE_BRANCH"` token accepted) — criterion: skill-prescribed `--base` denial lines (cycle 8: 1) → 0 across every PR opened this cycle; `test-cage-invariant-enforce-base-branch.sh` green; wrong-base / missing-base still denied (cage). Deterministic — one B/A/D execute each opens a PR with the prescribed form.

Backlog re-rank (friction is still the row with signal — 42 lines in cycle 8, 51 in cycle 7, 22 in cycle 6; the hook-denial class held at 16 but its composition flipped: `enforce-base-branch.py` text-scanning ANY Bash text containing the PR-create phrase is now the dominant sub-class with 7 lines and the other three hooks' 9 lines are pre-#1321; the SECOND class — skill prose that contradicts the caller or the runtime — is 14 lines across five recurring shapes and has never been on a slate; cost is slate-shaped (median 18.6M, B row 64M) and cannot move this cycle without a B; gate yield had 2 real catches, one of them the #1322 rule's own "subject" under-specification):
1. **#52** — `enforce-base-branch.py` decides from raw text: `is_create = re.search(r"\bgh\s+pr\s+create\b", command)` fires on a read-only grep pattern, a Python replacement string, plan/commit/heredoc prose (7 lines; the orchestrator's Step-6 tracker edit had to detour through the Write tool). `hooks/command_mask.py` (`segments()` + `head_index()`, #1321) already gives the three sibling hooks command-word matching; apply it here. D.
2. **NEW #59** — prose-vs-runtime contradictions, 14 lines / 5 shapes, every one a one-sentence edit: plan-issue Steps 5/6 say the draft goes under `.claude/logs/plan-drafts/` (4 lines; the caller says `.claude/scratch/`, `post-plan.sh` accepts any path, and the logs write ignores `PIPELINE_LOGS_ENABLED`, #25); plan-issue Step 6 "YOU MUST use the Write tool (not heredoc)" vs the bypass-mode system note (3); `filter-trusted-comments.sh --json` emits `{"comments":[…]}` with no `body` key while the plan-eval / plan-issue Step-1 prose implies a bare array + body (3, #56); evaluate-issue-pr Step 4 "Typecheck always runs" is a silent no-op when `PIPELINE_TYPECHECK_CMD` is unset (2, #18); plan-issue Step 5's `superpowers:writing-plans` mandates a header / `docs/superpowers/plans/` save / hand-off question that the canonical format overrides (2). Plus the two orchestrator one-liners of the same class: evolve Step 3's `path-hint=A|B|C|D` marker vocabulary (#55; classify accepts `A|B|C` only, D is `<!-- pipeline:path=D -->`) and the fullsend `## Greenlight matrix` source column citing the raw comments read (#56). A.
3. **#53** — the #1322 rule's "untouched" test is under-specified: the #1323 executor classed `test-plugin-hooks-runtime-config.sh` (greps `hooks/*.py`, a touched path) as pre-existing, CI went red, the evaluator fixed it; the PR body listed no `PRE-EXISTING:` files although the rule requires the line. Define "subject" as any test that reads, greps or execs a touched path, and make the `PRE-EXISTING:` list mandatory in the PR body (pr-eval checks it). A.
4. #39 — `create-checkpoint-tag.sh:95-99` `git checkout` + `git pull --ff-only` on the main checkout (fourth recurrence; it is the mechanism that flips live hooks mid-cycle). D, next cycle.
5. #46 — attachment ingest: `fetch-issue-attachments.sh` is trust-safe by construction (it runs `is-trusted-author` per comment, drops untrusted bytes, fails closed without the helper) and the cage test does not pin the hook's deny arm — the arm is stale, not a boundary. D, next cycle (deny-set narrowing on a trust hook deserves its own slate slot and issue body).
6. #57 (chunk-skip teeth) waits for a B measured under #1322; #51 needs an operator-launched calibration run; #13, #42, #41, #50 unchanged behind the above.

Slate for cycle 9 — 2 A + 1 D, 0 B, 0 C:
- **#52 → PATH D** `fix(hooks): enforce-base-branch.py decides from the command head, not raw text`. Hypothesis: the hook only needs to see a `gh pr create|edit` COMMAND (dequoted segment head via `command_mask.segments()`); prose, patterns and replacement strings are not commands, so masking them removes every cycle-8 denial without loosening the base-branch cage. Metric: friction — PR-create text-scan denial lines 7 → 0; cage test green; missing-base / wrong-base still denied. Measured by: retro (next cycle). Hook LOC +≤ 30.
- **#59 → PATH A** `docs(skills): align plan-issue / evaluate-issue-pr / evaluate-issue-plan / fullsend / evolve prose with the runtime`. Hypothesis: the 14 non-hook friction lines are prose that names a path, a tool or a shape the runtime no longer has; correcting the sentences (not adding rules) removes the lines and the retro's "doc/behaviour contradictions" row. Metric: friction — those five classes 14 → ≤ 2; contradictions row 4 → ≤ 1; net words ≤ 0. Measured by: retro (next cycle).
- **#53 → PATH A** `docs(skills): define "subject" for the CI-oracle untouched rule; PRE-EXISTING list mandatory in the PR body`. Hypothesis: the mis-class came from an undefined term, not from the rule; a ≤ 60-word definition plus a pr-eval check of the list closes the escape without re-adding the proof work #1322 removed. Metric: gate yield / escapes — CI-red heads from a mis-classed untouched test 1 → 0; PR bodies carrying `PRE-EXISTING:` lines when chunks fail 0/1 → 1/1. Measured by: retro (next cycle) and the #1322 quality-loss arm.

Why this slate: #52 is the largest single friction sub-class and the last of the four text-scanning guards without `command_mask.py` (the three masked ones are under verdict this cycle — a clean split); #59 is the largest class that has never been attacked and costs only edited sentences; #53 is the one real gate catch that the loop's own rule produced. No B: the only B-sized backlog item (#57) needs a #1322 measurement first — the B GREEN verdict carries one cycle. Deferred: #39, #46, #51, #57. Clone runs `strict`; wrapper-driven (`PIPELINE_HEADLESS=true`); forward-sync at Step 0 was a no-op (HEAD a1ac764, `origin/staging` already an ancestor). Plan-waves note: #59 and #53 both touch `skills/evaluate-issue-pr/SKILL.md`, so the planner will serialize them — dispatch the second the moment its edge partner merges (cycle-8 practice), not after the whole wave.

Dispatch discipline this cycle (so the deferred verdicts measure something): no hook-workaround hints in dispatch prompts (#1321 / #1323 test — agents must hit or not hit the hooks on their own); no "skip the pre-existing proof" coaching (#1322 test — the rule must be read from the skill bodies); Write-tool authoring of every issue body that spells the PR-create phrase (the pre-#52 hook is live until #52 merges).

## Post

**Merged 3/3.** #1328 (A, single opus, 2 plan rounds) PR #1331 → 3032ed0 · #1327 (D collapsed, sonnet) PR #1330 → 95dea65 (evaluator fix `fa1bfaa`) · #1329 (B split-role red:opus/green:sonnet, 1 plan round, `ELIGIBLE=high-blast` advisory under `SCOPE=all`) PR #1332 → d36b836. Classifier routed #1329 to B over its A hint (new prose-pin test file = a red/green cycle) while #1328 — same "update the pins" shape — stayed A; both merged, so the slate became 1 A + 1 B + 1 D and the B row exists for the #1322 GREEN criterion. Pipelined as in cycle 8: #1329 (wave 2, shares `evaluate-issue-pr` with #1328) was cut from 3032ed0 the moment #1328 merged, while #1327's pr-eval was still running. `DISPATCH=match` ×3, `ACTION=complete` ×3 first check, `BASE=ok` ×2, clean-main `untracked-only` at every per-dispatch and boundary check. Wall clock 11:21 → 12:33Z (≈72 min; cycle 8: 106, cycle 7: 77). Fourth wrapper-driven cycle (`PIPELINE_HEADLESS=true`), no operator action.

**Cost rows (Step 5, backfill ran):** #1327 16.8M stages=2 · #1328 34.1M stages=5 · #1329 29.6M stages=5. Loop-own median 29.6M (cycle 8: 18.6M; cycle 7: 37.4M; cycle 6: 19.6M). Per-path vs cycle 8: B 64.0M → 29.6M (−54 %, but cycle 8's B carried a 195k-output planner and 2 plan rounds; this one 1 round), A 18.6M → 34.1M (+83 %, 2 rounds both times; 9 files vs 3), D 6.8M → 16.8M (+147 %: the collapsed-D ran the full 4-chunk suite at 68 tool uses and its evaluator ran 13 guard pairs plus a CI-red fix at 38). Per-agent tool uses: #1327 executor **68** / 139k output, pr-eval 38 / 102k. #1328 classify 7, plan 29 / 122k, plan-eval 38 / 98k, re-plan 8, plan-eval r2 12, executor **58** / 103k, pr-eval 29 / 88k. #1329 classify 9, plan 32 / 97k, plan-eval 17 / 73k, RED 14 / 58k, GREEN **66** / 130k, pr-eval 20 / 83k. All three executors ran the full 4-chunk suite in the worktree; the #1328 executor also proved its untouched failures against the base checkout (accidentally — see the `run-test-suite.sh` finding below).

**Gate yield — 1 real defect fixed pre-merge (cycle 8: 2):** #1327's head went CI-red on `tests/test-plugin-hooks-phase2-coexistence.sh` — the test copies hooks into a sandbox and the rewritten `enforce-base-branch.py` imports `command_mask`, which the sandbox copy did not co-locate (`ModuleNotFoundError`). The collapsed-D executor's PR body said "all failures confirmed PRE-EXISTING" with no file list, and that test execs `hooks/*.py` — the SAME mis-class shape as #1323 in cycle 8, one cycle after the rule landed and while #1329 (the definition of "subject") was still in flight. Evaluator fix `fa1bfaa` (3-line co-location), rebased, CI green. plan-eval Revise-first 1/2 (#1328 round 1: a false claim about quoted heredocs in the very sentence being corrected; round 2 applied verbatim, approved; #1329 approved round 1 with a non-blocking rationale error). Split-role gate `additive-ok-ci-green`; #1329's GREEN touched nothing under `tests/`.

**Leaks:** 0 test-leaked worktrees across 6 suite-running agents (`git worktree list` = the three issue worktrees only at every probe). Five evaluator/planner `mktemp -d` fixtures left under `/tmp` and one under `.claude/scratch/proto-1329/` — every cleanup `rm -rf "$VAR"` was denied (below). Host: main checkout HEAD moved a1ac764 → 95dea65 mid-cycle with no orchestrator checkout/pull (backlog #39, **fifth recurrence**; between the #1327 merge and the #1329 GREEN's return — so the #1329 pr-eval ran under the post-#1327 hook and the GREEN under an unknown one); fast-forwarded to d36b836 by hand before this commit.

**Escapes:** hotfix 0 · revert 0 · later-fix 0 (the evaluator fix is a pre-merge catch; `run-retro.sh` counts `later-fix = 2` from its own substrate — the two evaluator commits `fa1bfaa` and the #1328 executor's restore).

**Mass:** words 56230 → 56329 (+99 = #1328 −12 ✓ on its ≤ 0 budget [plan-issue −28, evaluate-issue-pr +15, evaluate-issue-plan +1, fullsend −4, evolve +4] + #1329 +111 ✓ on ≤ +120 [execute +60, evaluate-issue-pr +51]) · hooks 14, hooks LOC 4393 → 4455 (**+62** = #1327 118/56 on `enforce-base-branch.py` vs its ≤ +30 budget ✗ — the raw-text decision became a segment walk plus a `_legacy_scan` fallback; the pattern from cycle 8: masking rewrites cost 2–3× their LOC estimate) · tests 441 → 442 / 80053 → 80161 LOC (+62 new prose-pin test, +43 Cases L–O, +3 co-location) · scripts 91 / 23091 unchanged · prose-pinning 184 → 185 `SKILL.md` greps, 46 `CLAUDE.md` · archaeology 352/135 → 352/136.

**Friction:** 48 `HARNESS-FRICTION:` lines — 42 from agents, 6 orchestrator (cycle 8: 42; cycle 7: 51). Classes: (1) **hook denials — 19 lines** (cycle 8: 16): `enforce-base-branch.py` text-scan **7** (grep patterns ×3, a commit-message heredoc, JSON payloads/probe strings ×2, an eval-comment heredoc — every one under the pre-#1327 hook; the only agent that ran wholly under the fixed hook, the #1329 pr-eval, reported none); `block_deletions.py` **5** — all ONE shape: `rm -rf "$VAR"` where `$VAR` holds the agent's own `mktemp -d` or `.claude/scratch/` fixture (the #1321 carve-out covers literal `/tmp` targets only; a variable is unresolvable in the hook, so it fails closed) — the grep-pattern / `sed -i` / heredoc-prose / awk-program shapes #1321 masked: **0** (cycle 8: 6); `enforce-comment-trust.py` **6** = 5 TRUE positives (five agents' first move was a raw `--json …comments…` read, correctly denied, then the trust filter) + 1 FP (a `python3 - <<'EOF'` body containing the greenlight table cell — the interpreter-owned heredoc residual, same as `restrict_paths`); `restrict_paths.py` **1**, `find / -path …` — a real `/` operand, by design (cycle 8: 3 FP → 0). (2) **NEW — `CLAUDE_PLUGIN_ROOT` in worktrees, 5 lines:** the dispatch prompts named the main checkout, but `_resolve-plugin-root.sh` under `PIPELINE_USE_LOCAL_PLUGIN=true` resolves `git rev-parse --show-toplevel` = the WORKTREE (correct — the worktree mirrors the scripts); the orchestrator's claim was wrong, and the real defect underneath: `run-test-suite.sh` derives `REPO_ROOT` from its own script dir, so with the main-checkout value the #1328 executor's chunk 1 ran the MAIN checkout's tests, not the worktree's. (3) prose-vs-runtime (the #1328 classes) — 8, all from agents that ran BEFORE 3032ed0 (plan-drafts ×3, `--json` shape, writing-plans overrides, Write mandate, typecheck no-op, `Approve` vs `Approved`); verdict next cycle. (4) plan / issue-body errors — 6 (#1328's Task 8 omitted `test-plan-issue-path-c-autopost.sh`, whose pins the plan's literal wording broke — the executor restored +2 words; #1329's word-budget recipe counts the `+` diff marker; the issue's "greps `hooks/*.py`" claim was wrong for #1323's test; mawk `\s`). (5) tooling — 4: `gh pr checks --watch` returns "no checks reported" for ~10 s after a push (twice); `superpowers:code-reviewer` not registered (GREEN used `general-purpose`); classify row 4 routed the same shape to A (#1328) and B (#1329). Orchestrator: 1a attachment ingest (#46, fourth cycle), #39 pull, the #1322 second mis-class, the classify inconsistency, the wrong `CLAUDE_PLUGIN_ROOT` claim in six dispatch prompts (own), and every issue body / diagnose file authored through the Write tool to dodge the pre-#1327 hook.

**Verdicts:**
- **#1321 no-effect** — three-hook denial lines 9 → 12, FP 8 → 6 (−25 %, under the 30 % bar); the shapes the issue masked went 6 → 0 and true positives held (cage 7/7, the evaluator's three fail-closed fixes stood), but one residual shape — `rm -rf "$VAR"` on the agent's own fixture — is now 5 of the 6 FP lines and was never coverable by a literal-path carve-out. New hypothesis #60 (scratch-dir carve-out + skills prescribe fixtures under `.claude/scratch/`) replaces #45/#48, which move to the bottom.
- **#1322 no-effect** — none of the four Step-2 criteria moved ≥ 30 % the right way: A executor 46 → 58, pr-eval 13/29 → 29/38 (A/D), plan-eval 29/21 → 38/17, A-row 18.6M → 34.1M; B GREEN 90 → 66 (−27 %, and still > 60, the cycle-8 "needs teeth" line); every executor still ran the full 4-chunk suite (the rule removes the proof, not the run) and the quality-loss arm fired (#1327's second mis-classed subject test, CI red, evaluator-caught). #49 moves to the bottom; #57 (`run-test-suite.sh --changed-only` chunk skip when base CI is green) is the follow-up, now with #1329's subject definition to bound it.
- **#1323 confirmed** — skill-prescribed `--base "$PIPELINE_BASE_BRANCH"` denial lines 1 → 0 across three PRs opened with the prescribed form; `test-cage-invariant-enforce-base-branch.sh` green on every head.
- **#1327 / #1328 / #1329** — mass verdicts now: #1328 −12 words ✓; #1329 +111 ✓; #1327 hooks LOC +62 ✗ vs ≤ +30 (cage green ✓, `human` 0 ✓). Friction/gate verdicts deferred to cycle 10 (`verdict-candidates: 1329 1328 1327`): #1327 → PR-create text-scan denial lines 7 → 0 (first cycle with the hook live from Step 0); #1328 → the five prose classes 8 → ≤ 2 and the contradictions row; #1329 → CI-red heads from a mis-classed untouched test 1 → 0 AND every PR body carries the `PRE-EXISTING:` list (the #1332 body already did — 17 lines).

**Usage:** start five_hour=1 seven_day=47 → end five_hour=17 seven_day=49 (16 five-hour points; EST5 was 27).

**Hand-off (cycle 10 candidates):** #60 block_deletions fixture-cleanup carve-out (5 lines, D); #61 `run-test-suite.sh` `REPO_ROOT` from the script dir runs the wrong tree under dogfood + the fullsend dispatch prompt must not name the main checkout as `CLAUDE_PLUGIN_ROOT` inside a worktree (5 lines, D); #57 verification-load teeth (B GREEN 66, D executor 68 — the rule needs a mechanical skip, B); #39 checkpoint-tag pull (fifth recurrence, D); #62 classify row 4 A/B inconsistency on "update the prose pins" (A/D); #63 raw `--json comments` first attempts ×5 — one sentence at the Step-1 fences naming the denied form (A); #46 attachment ingest (D). Cost: the D row tripled because the collapsed-D ran the whole suite — #57 is the fix for D too.

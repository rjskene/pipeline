cycle-issues: 
pending-verdicts: 1317 1316 1315
verdict-candidates: 

friction: denials = n/a (tool-use.log has no decision field; hooks/log-tool-use.sh logs invocations only)
friction: harness-friction-lines = 51
friction: harness-friction-window = tracker cycle 7 comment
friction: compactions = n/a (no transcript substrate)
friction: hotfix = n/a (no cycle window)
friction: manual-merge = n/a (no cycle window)
friction: human = 0

HARNESS-FRICTION: SKILL.md step 2 (`LATEST_CLASS_TS`) and step 7 (`CLASS_COUNT`) prescribe `gh issue view --json comments` with `.authorAssociation`/`.createdAt` | that form is hard-denied; ported both to `gh api repos/<repo>/issues/<N>/comments` with `.author_association`/`.created_at` (exactly the drift #1315 itself is filing) (#1315 classify)
HARNESS-FRICTION: SKILL.md Boot block prescribes `${HOME}/.claude/plugins/cache/...` glob fallbacks for `_cpr_dir` | not needed and blocked by restrict_paths here; `source ./scripts/_resolve-plugin-root.sh` after `pipeline.config` resolved `CLAUDE_PLUGIN_ROOT` to the working tree directly (#1315 classify)
HARNESS-FRICTION: SKILL.md step 2 + step 7 use `gh issue view --json comments` | hook hard-denies that form; ported to `gh api repos/.../issues/N/comments` with `.author_association` / `.created_at` (#1317 classify)
HARNESS-FRICTION: SKILL.md Boot block pastes `${HOME}/.claude/plugins/cache/...` glob fallbacks | restrict_paths blocks other-home path tokens; used `source ./scripts/_resolve-plugin-root.sh` directly per dispatch note (#1317 classify)
HARNESS-FRICTION: skill step 2 and step 7 use `gh issue view --json comments` | hard-denied by hook; ported to `gh api repos/.../issues/N/comments` with REST field names, worked fine (#1316 classify)
HARNESS-FRICTION: skill Boot block pastes `${HOME}/.claude/plugins/cache/...` glob fallbacks | restrict_paths would block those tokens; `source ./scripts/_resolve-plugin-root.sh` alone resolved `CLAUDE_PLUGIN_ROOT` correctly under `PIPELINE_USE_LOCAL_PLUGIN=true` (#1316 classify)
HARNESS-FRICTION: Blast-radius rule says "exclude tests/ from the file count" and yields a D prior | for a test-side `fix(` the excluded files ARE the work (~19 test files); the rule's count is meaningless for this shape and only row-5 patch-size judgment prevented a false D (#1316 classify)
HARNESS-FRICTION: plan-issue SKILL.md Steps 5/6 say write the draft to `.claude/logs/plan-drafts/` | caller directive requires `.claude/scratch/`; `post-plan.sh` accepts either path (#1317 plan)
HARNESS-FRICTION: plan-issue SKILL.md Step 4a fence uses `gh issue view --json comments` for the diagnosis-freshness probe | hook hard-denies that form; only relevant when `needs-debug` is set (not here) (#1317 plan)
HARNESS-FRICTION: plan-issue SKILL.md Step 3b says run `fetch-issue-attachments.sh` in interactive single-issue planning | caller hard-denies direct invocation; skipped (issue has no attachments) (#1317 plan)
HARNESS-FRICTION: SKILL.md Boot block says to paste `${HOME}/.claude/plugins/cache/...` glob fallbacks | with `PIPELINE_USE_LOCAL_PLUGIN=true`, `source ./scripts/_resolve-plugin-root.sh` alone resolves `CLAUDE_PLUGIN_ROOT`; the globs are blocked by restrict_paths and unnecessary (#1316 plan)
HARNESS-FRICTION: SKILL.md Step 6 says write the draft under `.claude/logs/plan-drafts/` | dispatcher requires `.claude/scratch/`; `post-plan.sh` accepts either (#1316 plan)
HARNESS-FRICTION: SKILL.md Step 4a fence uses `gh issue view --json comments` for the diagnosis-freshness probe | that form is hard-denied by enforce-comment-trust; not needed here (no `needs-debug`), but the fence must be ported to `gh api …/comments` + `.created_at` (#1316 plan)
HARNESS-FRICTION: issue #1315 Notes say the hook exempts the trust filter's internal fetch "by an exact-string check" (keep fetch string byte-identical) | `hooks/enforce-comment-trust.py` allows any command whose TEXT contains the substring `filter-trusted-comments.sh`; the script's internal `gh` call is never seen by the hook, so fetch-string identity is not load-bearing (#1315 plan)
HARNESS-FRICTION: plan-issue SKILL.md Step 5/6 say write the draft under `.claude/logs/plan-drafts/` | caller directive routes drafts to `.claude/scratch/` (followed the caller) (#1315 plan)
HARNESS-FRICTION: plan-issue SKILL.md Boot fence pastes `${HOME}/.claude/plugins/cache/...` glob fallbacks | those tokens are blocked by `restrict_paths.py`; `source ./scripts/_resolve-plugin-root.sh` after `pipeline.config` resolves the root without them (#1315 plan)
HARNESS-FRICTION: nothing warned that `sed -i 's/13 …/15 …/'` is a benign edit | `block_deletions.py` denied it as a "destructive deletion command" (used the Edit tool instead) (#1315 plan)
HARNESS-FRICTION: skill Step 1 says run `gh issue view --json comments` | the enforce-comment-trust hook hard-denies that form; had to rebuild `{comments:[…]}` from `gh api …/issues/N/comments` with jq (#1317 plan-eval)
HARNESS-FRICTION: `block_deletions.py` reports "destructive deletion" on `: > file` (bare truncate-to-create) | the command's only deletion-shaped token was a file-create idiom; the error message truncates the command at 120 chars so the real trigger (`: >`) was not visible — cost two blocked attempts to isolate (#1317 plan-eval)
HARNESS-FRICTION: Bash tool's grep is `ugrep`, which rejects `(a|b|)` empty-alternation ERE | GNU grep accepts it; pattern had to be rewritten (#1317 plan-eval)
HARNESS-FRICTION: skill Step 1 fence uses `gh issue view --json comments` | hard-denied; rebuilt `{comments:[…]}` from `gh api …/comments` with jq as the task brief prescribed (#1316 plan-eval)
HARNESS-FRICTION: `block_deletions.py` blocks `sed -i` (in-place edit) even on a throwaway tmp clone | had to rewrite via `sed … > tmp && cat tmp > file` (#1316 plan-eval)
HARNESS-FRICTION: `PIPELINE_PROJECT_ROOT` is exported to the live checkout in the agent's Bash env | any unpinned fixture test run from the checkout would have leaked — confirms the issue's root cause in situ; expected count "3" in the brief was stale because #1317's worktree was cut concurrently (#1316 plan-eval)
HARNESS-FRICTION: skill Step 1 fence says the block is hard-denied by enforce-comment-trust | the 3-site TRUST-THEN-ANCHOR blocks pass the hook when run as the single command the prose demands (helper name in the `is-trusted-author` loop triggers allow-first); only the 4 one-liner fences are actually denied (#1315 plan-eval)
HARNESS-FRICTION: `block_deletions.py` blocked a read-only `grep -rnE '...(<N>|"?\$)'` command as a "destructive deletion" | no deletion verb present — splitting into simpler grep calls passed (#1315 plan-eval)
HARNESS-FRICTION: operator note said expected worktree count 3 | observed 5 (two sibling feature worktrees from other cycle-7 issues, not this stage) (#1315 plan-eval)
HARNESS-FRICTION: execute-issue-plan Step 9b says pass `--base "$PIPELINE_BASE_BRANCH"` (quoted var) | `hooks/enforce-base-branch.py` text-scans the unexpanded token and hard-blocks it ("must target 'evolve', not '$PIPELINE_BASE_BRANCH'"); only the literal `--base evolve` passes, so the skill's prescribed form is un-runnable under this hook (#1317 execute)
HARNESS-FRICTION: skill Step 1 fence `gh issue view --json comments` | hard-denied by hook; used `gh api repos/.../issues/1317/comments` + `.author_association` remap into the `{comments:[...]}` shape `select-plan-comment.sh` expects (#1317 pr-eval)
HARNESS-FRICTION: `restrict_paths.py` blocked a `grep 'Verdict:\*\* Approve / Revise'` pattern as "path outside project boundary: /" | the bare ` / ` was a markdown literal from the eval-plan template, not a path (#1317 pr-eval)
HARNESS-FRICTION: skill Boot block pastes `${HOME}/.claude/plugins/cache/...` glob fallbacks | `PIPELINE_USE_LOCAL_PLUGIN=true` resolves `CLAUDE_PLUGIN_ROOT` to the worktree via `_resolve-plugin-root.sh` alone; the globs were unneeded (#1317 pr-eval)
HARNESS-FRICTION: system note says Bash cwd resets per call | memory notes say it persists — irrelevant here since every command was `git -C`/absolute-path anchored, but the two directives contradict (#1316 red)
HARNESS-FRICTION: the reference test shape (`trap 'rm -rf "$WORK_ROOT"' EXIT`) cannot be written via a Bash heredoc under `block_deletions.py` | had to author the file with the Write tool (prompt already hinted this; noting it as the durable rule for RED authors copying that shape) (#1316 red)
HARNESS-FRICTION: system prompt says "make file changes with sed, heredocs … rather than Write/Edit" | task directive (and the hook) requires Write/Edit for files spelling the denied fence literal — followed the directive (#1315 red)
HARNESS-FRICTION: `grep` on this host is ugrep 7.8.4; a pattern beginning with `--json…` is parsed as an option unless passed via `-e` | the plan's N.T/5d predicates start with a letter so they are unaffected, but any GREEN grep whose pattern starts with `--` must use `-e` (#1315 red)
HARNESS-FRICTION: prompt said hooks hard-deny absolute paths outside this repo | literal `/tmp/tmp.*/clone` paths in Bash commands were accepted (#1316 green)
HARNESS-FRICTION: prompt said 7 worktrees expected including mine | 6 observed before the suite (the prompt's own enumeration also sums to 6) (#1316 green)
HARNESS-FRICTION: Step 8b names `Agent(subagent_type: "superpowers:code-reviewer")` | not in the available agent list; used `general-purpose` with the reviewer template (#1316 green)
HARNESS-FRICTION: `check-branch-cruft` reported 11 changed paths vs `origin/evolve` | stale remote ref; 8 after `git fetch origin evolve` (#1316 green)
HARNESS-FRICTION: `run-test-suite.sh --chunk N/4` is documented as one foreground Bash call each | without an explicit `timeout` the call auto-backgrounds past the 120 s default and a `| tail` pipe swallows the runner's exit code (exit 0 hid a `RESULT=fail`); needs `timeout: 600000` and reading the `RESULT=` line directly (#1315 green, reported as "no friction" — transcribed by the orchestrator)
HARNESS-FRICTION: `block_deletions.py` hard-denies a trailing `rm -rf "$T"` on a `mktemp -d` path in the same command | throwaway fixture cleanup is blocked — temp dirs `/tmp/tmp.Ohc9BUsOFN`, `/tmp/tmp.C6NVaw1zTZ` were left behind (#1315 pr-eval)
HARNESS-FRICTION: `restrict_paths.py` treats an awk `/regex/` pattern operand as a filesystem path ("path outside project boundary") | it was an awk match pattern, not a path — had to rewrite as `$0 ~ "..."` (the prompt's "no `/`-leading awk regex literals" warning was accurate) (#1315 pr-eval)
HARNESS-FRICTION: SKILL.md Step 1 says `gh issue view <N> --json comments` is the fetch (base-branch prose, now superseded by this very PR) | the hook hard-denies that form; used `filter-trusted-comments.sh --json 1315`, which is exactly what the merged prose now prescribes (#1315 pr-eval)
HARNESS-FRICTION: SKILL.md Step 1 says fetch via `gh issue view --json comments` | the dispatch prompt hard-denies that form and requires `gh api .../comments` — the trust-gate block had to be re-shaped for the REST field names (`author_association`, top-level array) (#1316 pr-eval)
HARNESS-FRICTION: `block_deletions.py` blocks `rm -rf` on a `mktemp -d` path the evaluator itself created | the skill's own Executable-verification section directs building throwaway fixtures under `mktemp -d`, so post-eval cleanup of those fixtures is not possible from this role (#1316 pr-eval)
HARNESS-FRICTION: evolve SKILL.md Step 2 diagnose is a read-only step | `enforce-comment-trust.py` blocked a `grep -nE` whose PATTERN operand spelled the denied fetch form (text-scan of the whole command, backlog #45 class) (orchestrator, step 2)
HARNESS-FRICTION: #1282 says `restrict_paths.py` scans only real path arguments | the Step-3 awk program `/^## Cycle log/{…}` was blocked as "path outside project boundary" — a `/`-leading regex operand still classes as a path (orchestrator, step 3)
HARNESS-FRICTION: fullsend Step 1a prescribes a direct `fetch-issue-attachments.sh` call per slate issue | `enforce-comment-trust.py` hard-denies any command invoking it (exit 2 since #1294); no sanctioned form exists — `HEADLESS-DEFAULT: attachment-ingest decision=skip reason=hook-hard-denies-direct-call` (orchestrator, fullsend 1a)
HARNESS-FRICTION: fullsend/#1214 says the autonomous lane never pulls the orchestrator main checkout | `create-checkpoint-tag.sh` fast-forwarded it to e753719 after the first merge (#39, second cycle running); fast-forwarded to c72ed0b by hand before the retro commit (orchestrator)
HARNESS-FRICTION: evaluate-issue-pr Step 8 merges base into the branch | the #1316 evaluator rebased instead (RED SHA 6a8f42a→9289c29) to keep the split-role two-dot window clean (#42, recurring) (orchestrator)
HARNESS-FRICTION: evolve SKILL.md Step 4 / fullsend Step 4→5 imply the whole slate is approved before any execute wave | pipelining #1317 (A) through execute+merge while the B plan-evals ran was strictly faster and conflict-free; neither skill states a rule either way (orchestrator)
HARNESS-FRICTION: evolve SKILL.md Step 2 criterion for #1294 ("≥1 TP or FP share ≤ 50 % → confirmed") conflated hook-is-real with hook-earns-its-keep | the issue's hypothesis was the former; the FP share is backlog #12/#45's metric, so the verdict was taken on the stated metric (denials measurable, cage SKIP 4→0) with the FP finding attached (orchestrator, step 6)

delta prose-pinning tests/grep claude.md 8 (baseline 38 -> computed 46)
delta harness mass/words -15 (baseline 56100 -> computed 56085)
delta harness mass/skills 0 (baseline 19 -> computed 19)
delta harness mass/tests loc 10 (baseline 79900 -> computed 79910)
delta harness mass/hooks loc 7 (baseline 3900 -> computed 3907)
delta harness mass/scripts loc -9 (baseline 23100 -> computed 23091)
delta harness mass/hooks 0 (baseline 13 -> computed 13)
delta issue-number archaeology in skill bodies/refs 0 (baseline 351 -> computed 351)
delta issue-number archaeology in skill bodies/distinct n/a (baseline row not found: issue-number archaeology in skill bodies/distinct)
delta harness mass/tests n/a (baseline row not found: harness mass/tests)
delta prose-pinning tests/grep skill.md 18 (baseline 165 -> computed 183)
delta harness mass/scripts 0 (baseline 91 -> computed 91)
stage cost share: execute 33% · orchestrator 19% · pr-eval 16% · plan 14% · plan-eval 14% · classify 4%
gates (plan-eval + pr-eval): 30% of spend, ≈$810
split-role red+green (19 issues): 24% of spend, ≈$650
B-over-D ceremony premium: ≈$728 foregone
median PATH B PR: 320 LOC · 23M tokens · 44 min · ≈$55 · 67k tokens/LOC
pr-eval yield (Jun→Sep, 3 repos, 56 evals): 0 Flagged (pilot era 2/30); evolve cycle 6: 1 real defect fixed pre-merge by the evaluator (#1282 scratchpad-segment boundary escape, CI green, cage test blind to it)
plan-eval Revise-first rate: 35% pipeline · 21% bomon-web · 28% work-orchestrator
staging CI after merge: 2 red / 398 pushes, none since June
known escape class: issue 1199 — pr-eval Approved + CI green, 9 false-green assertions; 2 sibling suites likewise
doc/behaviour contradictions (one session): 4
weak-model pass (strict + sonnet, 5-issue calibration slate): strict: 3/5 reftest, 3516s (2026-09-06 run #2; #7 D + #8 B unrun — orchestrator held). lean + opus PATH B executor: 2/5 reftest, 4577s (2026-09-08 run #3; A merged with reftest=fail, D pass, B pass +1 unexpected file, B pr-open + B unrun — print-mode background ceiling killed the session mid-wave-2, #1306). Same-input docs issue: strict pass → lean fail.
loop-own tokens/issue (median, all captured stages): 37.4M (cycle 7: #1315 51.7M B 1 plan round · #1316 37.4M B 1 round · #1317 11.1M A 1 round — verification load, not plan rounds, is the driver, backlog #49; cycle 6: 19.6M; cycle 5: 64.6M; cycle 4: 47.8M; scorecard median PATH B PR 23M)

escapes: hotfix = n/a (no cycle window)
escapes: revert = n/a (no cycle window)
escapes: later-fix = n/a (no cycle window)

gate-yield: Flagged/evals = 0/0
gate-yield: Revise/plans = 0/0

weak-model pass: 2/5

usage: five_hour=9% seven_day=33% threshold=85


## Diagnose

Verdicts resolved at Step 2:
- **#1306 confirmed** — calibration run #4 (2026-09-11, strict + sonnet, launched with `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0`) ran 4158 s end-to-end with **0** `Background tasks still running after 600s` terminations in `docs/retros/calib/2026-09-11.log` (run #3: 1, at the 600 s ceiling, mid-wave-2). The headless orchestrator waited on every in-flight agent and completed the slate pass. The 2/5 reftest is not a harness signal: run #3's stale sandbox PR #22 survived `--reset` and the orchestrator correctly deferred #23/#26/#27 as superseded — backlog #51 (`cmd_reset()` must close open PRs + delete remote branches before reaping issues). No clean strict baseline exists yet; run #5 after #51 lands.

Verdicts carried from cycle 7 (all three resolve at Step 5 of THIS cycle — cycle 7's agents ran before/while their fixes merged, so cycle 8 is the first window that measures them; `run-retro.sh` printed `pending-verdicts: 1317 1316 1315`, matching the cycle-7 comment):
- **#1315** (seven fences → `filter-trusted-comments.sh --json`) — criterion: raw-fetch friction lines (cycle 7: 10) → 0 AND skill-prescribed `enforce-comment-trust` denials (cycle 7: 2) → 0 across the cycle-8 agent reports. ≥1 raw-fetch line naming a skill fence → no-effect.
- **#1316** (fixture tests pin `PIPELINE_PROJECT_ROOT`; post-suite leak guard) — criterion: leaked live worktrees after every suite run (cycle 7: 9, five caught by the guard mid-chunk) → 0; `git worktree list` probed after each GREEN and each pr-eval. Any `wt-` leak from a fixture test → no-effect; the guard firing on a NEW producer counts as confirmed-with-finding.
- **#1317** (plan-round binding rule in the skill bodies) — criterion: every cycle-8 plan approved in ≤ 2 rounds WITHOUT the binding rule restated in any dispatch prompt (cycle 7 was 1/1/1 WITH coaching, so it proved nothing). Dispatch prompts this cycle carry only the fullsend-prescribed content; no plan-round coaching. A 3-round plan → no-effect.

Backlog re-rank (top of the list; friction is still the row with signal — 51 lines in cycle 7, 22 in cycle 6, 45 in cycle 5 — and the dominant class is now **hook denials on benign commands**, 13 lines, false-positive share ≈ 10/13; cost is the second signal — loop-own median 19.6M → 37.4M with plan rounds already at 1, so verification load is the driver):
1. #45 + #48 — `block_deletions.py` (41 LOC) text-scans the whole command: 8 of the 13 denial lines (in-place `sed` ×2, colon-truncate file-create, a read-only `grep -rnE`, recursive-remove of an agent's own `mktemp -d` path ×3, heredoc prose spelling a `trap` cleanup); `restrict_paths.py` classes awk `/regex/` program operands and a grep ` / ` literal as paths (4 lines; the #1282 residual); `enforce-comment-trust.py` text-scans a grep PATTERN operand (1 line). One masking discipline fixes all three: strip heredoc bodies and quoted-string / pattern operands before matching, match deletion verbs in command-word position only, allow recursive-remove when every target is under `${TMPDIR:-/tmp}`. Cage invariants (`tests/test-cage-invariant-*.sh`, never edited by the loop) pin the true positives, so the narrowing cannot widen the cage.
2. #49 — verification load: both cycle-7 GREENs ran the full 4-chunk suite, then proved 14–19 failing files pre-existing against a throwaway `origin/evolve` clone (~20 tool uses each; #1315 GREEN 86 tool uses / 199k output tokens); both plan-evals prototyped the whole plan in throwaway copies (62 and 38 tool uses). CI on the pushed head is already the oracle for pre-existing failures.
3. #47 — `enforce-base-branch.py` hard-blocks the exact `--base "$PIPELINE_BASE_BRANCH"` form execute-issue-plan Step 9b / fullsend Step 6a prescribe (the regex reads the unexpanded token as the branch name); only the literal passes, so every executor detours. 1 line per B issue, deterministic.
4. #46 — fullsend Step 1a's attachment ingest is hard-denied by the hook it sits beside (orchestrator `HEADLESS-DEFAULT: attachment-ingest decision=skip` every cycle). D, next cycle.
5. #51 — `calibration-run.sh --reset` leaves open sandbox PRs; blocks the strict baseline. D; its verdict needs an operator-launched run #5, so it waits for a cycle with a fresh 5h window.
6. #39 (checkpoint-tag pulls the main checkout — recurred ×2), #42 (split-role two-dot — recurred ×2 as rebase), #41, #50 (chunk-run `timeout` note): D/A sized, behind the above.
7. (unchanged order below.)

Slate for cycle 8 — 1 B + 1 A + 1 D, 0 C:
- **#45+#48 → PATH B** `fix(hooks): argument-position matching in block_deletions / enforce-comment-trust / restrict_paths`. Hypothesis: the four newly-real guards deny mostly benign commands because they regex the raw command text; masking heredoc bodies + quoted/pattern operands and matching verbs in command-word position drops the false-positive share without losing a true positive. Metric: friction — denial lines 13 → ≤ 4, FP share ≈ 10/13 → ≤ 2 lines; cage tests 7/7 green unchanged. Measured by: retro (next cycle).
- **#49 → PATH A** `docs(skills): CI is the oracle for pre-existing failures; plan-eval prototypes the RED ledger only`. Hypothesis: the GREEN / pr-eval pre-existing-failure proof and the plan-eval whole-plan prototype are the cost driver now that plan rounds are 1; a ≤ 150-word rule in execute-issue-plan, evaluate-issue-pr and evaluate-issue-plan removes ~20 tool uses per agent. Metric: loop-own tokens/issue median 37.4M → ≤ 25M; GREEN + pr-eval tool uses. Measured by: retro (next cycle) cost rows.
- **#47 → PATH D** `fix(hooks): enforce-base-branch.py accepts the unexpanded PIPELINE_BASE_BRANCH token`. Hypothesis: the hook resolves `EXPECTED_BASE` from the same config the shell variable expands from, so the literal `$PIPELINE_BASE_BRANCH` / `${PIPELINE_BASE_BRANCH}` token is equal-by-construction and must be allowed (env value checked when present). Metric: friction — skill-prescribed `--base` denial 1 → 0; cage test green. Measured by: retro (next cycle).

Why this slate: #45/#48 is the largest single friction class and the first one that costs real denials rather than detours (13 of 51 lines); #49 attacks the row that doubled (cost) with a docs-only change, so its verdict is cheap; #47 is a deterministic one-line-per-issue denial with a one-regex fix. All three move a named scorecard row (friction ×2, loop-own tokens/issue ×1). Deferred: #46, #51, #39, #42, #41, #50. Clone runs `strict`; wrapper-driven (`PIPELINE_HEADLESS=true`); forward-sync at Step 0 was a no-op (`origin/staging` already an ancestor of `evolve`, HEAD 0b668ab).

Dispatch discipline this cycle (so the deferred verdicts measure something): no plan-round coaching in dispatch prompts (#1317 test); no comments-fetch porting hints (#1315 test); `git worktree list` probe after every suite-running agent (#1316 test).

## Post

**Merged 3/3.** #1322 (A, single opus, 2 plan rounds) PR #1324 → a9b0a55 · #1323 (D collapsed, sonnet) PR #1325 → 5c36b16 · #1321 (B split-role red:opus/green:sonnet, 2 plan rounds, `ELIGIBLE=high-blast` advisory under `SCOPE=all`) PR #1326 → 19d2ef6. Pipelined: #1322 planned/evaluated/executed/merged while #1321's plan went through its two rounds; #1323 (D) was dispatched the moment #1322 — its only `plan-waves.sh` edge partner — merged, not after the whole of wave 1. `DISPATCH=match` ×3, `ACTION=complete` ×3 first check, `BASE=ok`, clean-main `untracked-only` at every per-dispatch and boundary check. Wall clock 18:08 → 19:54Z (≈106 min; cycle 7: 77, cycle 6: 90). Third wrapper-driven cycle (`PIPELINE_HEADLESS=true`), no operator action.

**Cost rows (Step 5, backfill ran):** #1321 64.0M stages=5 · #1322 18.6M stages=5 · #1323 6.8M stages=2 (collapsed D: execute + pr-eval are the only attributable dispatches). Loop-own median 18.6M (cycle 7: 37.4M; cycle 6: 19.6M) — but the median is slate-shaped (an A and a D); the B row is the heaviest yet (#1315: 51.7M). Per-agent: #1321 planner 61 tool uses / 195k output (probed all three hooks live), RED 53, GREEN **90** tool uses / 265k output (full 4-chunk suite, `gh run list` base-CI proof, self-review subagent), plan-evals 29 + 21, pr-eval 35 (~70 hook probes + fuzz). #1322 executor 46 (cycle-7 GREENs: 86), pr-eval 13. #1323 executor 45, pr-eval 29. The #1322 rule landed mid-cycle (a9b0a55) — the #1321 GREEN's worktree was cut before it and rebased onto it mid-run, so its 90 tool uses are not a clean read of the rule; cycle 9 is.

**Gate yield — 2 real defects fixed pre-merge by evaluators (cycle 7: 0):** #1323's head `fa9d7c6` went CI-red (`test-plugin-hooks-runtime-config.sh` greps the hook for the literal braced placeholder; the new Python literal tripped it) — the collapsed-D executor had classed it as "pre-existing/untouched" although the test reads `hooks/*.py`; evaluator fix `38b1c01`. #1321's evaluator found three fail-closed regressions vs the pre-#1321 deny set (`\rm -rf` alias bypassed the `_HEAD` anchor; `xargs`-wrapped rm qualified for the `/tmp` carve-out; brace/glob targets `/tmp/{x,../home}` passed `_is_tmp` literally) — fix `5548d3a` + 5 deny controls in the shared `test_block_deletions.py`. plan-eval 2/2 Revise-first (cycle 7: 0/3) — both round-1 plans carried a concrete defect (a word-count formula that fails on a correct implementation; a heredoc-mask test case contradicting its own spec + a double-quoted `$(…)` true-positive loss + two unspecified branches); both round 2s applied the prescriptions verbatim (diff-verified by the round-2 evaluators) and approved.

**Leaks:** 0 worktrees leaked across 6 suite-running agents (`git worktree list` = own worktrees only at every probe; cycle 7: 9). No `LEAK:` lines. Two evaluator scratch dirs left under `.claude/scratch/` (cleanup denied by `block_deletions.py` — the pre-#1321 hook; gitignored). Host: main checkout HEAD moved `0b668ab → a9b0a55 → 5c36b16` mid-cycle with no orchestrator checkout/pull (backlog #39, fourth recurrence); fast-forwarded to 19d2ef6 by hand before this commit.

**Escapes:** hotfix 0 · revert 0 · later-fix 0 (the two evaluator fixes are pre-merge gate catches, not escapes; `run-retro.sh` counts `later-fix = 1` from its own substrate).

**Mass:** words 56085 → 56230 (+145 = #1322's three same-line rules, on its ≤ +200-token budget) · hooks 13 → 14 (`hooks/command_mask.py`), hooks LOC 3907 → 4393 (**+486**: #1321 ≈ +464 incl. the evaluator's three fixes vs the issue's soft +≤150 — the plan flagged +175; #1323 +22 vs +≤15) · tests 441 / 79910 → 80053 LOC · scripts 91 / 23091 unchanged · prose-pinning 183 → 184 `SKILL.md` greps · archaeology 351/135 → 352/135.

**Friction:** 42 `HARNESS-FRICTION:` lines — 34 from agents, 8 orchestrator (cycle 7: 51; cycle 6: 22). Classes: (1) **hook denials on benign commands — 16 lines**, but the composition changed: `enforce-base-branch.py` text-scanning ANY Bash text containing `gh pr create` (grep patterns, a Python replacement string, plan/commit prose) is the new dominant sub-class with **6** agent lines + 1 orchestrator (0 in cycle 7 — it surfaced because three agents had to grep skills for the phrase); `block_deletions.py` 5 (read-only greps whose pattern spelled the verbs ×2, in-place `sed` on a throwaway ×1, own-fixture recursive-remove ×1, `git checkout --` ×1); `restrict_paths.py` 3 (interpreter-owned `python3 - <<EOF` heredoc still scanned, the orchestrator's awk program operand, `/tmp/<file>` via the Write tool); `enforce-comment-trust.py` 1 (fullsend 1a, #46). All 16 are pre-#1321 hooks (the fix merged last, 19d2ef6). (2) `.claude/logs/plan-drafts/` vs `.claude/scratch/` — 4 (recurring, #25). (3) Write-vs-heredoc directive conflict (skill says Write, bypass-mode system note says heredoc) — 3. (4) `filter-trusted-comments.sh --json` shape `{"comments":[…]}` undocumented / no body — 3. (5) `PIPELINE_TYPECHECK_CMD` unset → "always runs" no-op — 2 (#18). (6) chunk `RESULT=` hidden by output overflow — 1 (#50). (7) `superpowers:writing-plans` mandates header/save/handoff that plan-issue overrides — 2. (8) skill-prescribed `--base "$PIPELINE_BASE_BRANCH"` denied — 1 (#47, fixed by #1323 mid-cycle). Orchestrator: `path-hint=D` rejected by classify (evolve skill vocabulary wrong), `plan-waves.sh` "do NOT change" mention counted as a file edge, awk-program FP, 1a attachment ingest, #39 pull, and the #1322 "subject" under-specification that let #1323's executor mis-class a touched test.

**Verdicts:**
- **#1315 confirmed** — raw comments-fetch friction lines 10 → 0 (no cycle-8 agent hit a skill fence prescribing the denied form; the only mention is the #1323 evaluator's "implied" read of evaluate-issue-pr Step 11.3, which prescribes nothing), skill-prescribed `enforce-comment-trust` denials 2 → 0. Residual finding: the fullsend `## Greenlight matrix` table still cites `gh pr view --json comments` in its *source* column (doc only — `auto-merge-gate.sh` does the read); `filter-trusted-comments.sh --json` emits `{"comments":[…]}` with no `body` key and three agents tripped on the undocumented shape (backlog).
- **#1316 confirmed** — leaked live worktrees 9 → 0 across six suite-running agents; the guard had nothing to fire on. Pre-existing-failure proofs still happened (3 agents: #1322 executor, #1321 GREEN, #1323 executor) but via `gh run list` base-CI checks, not throwaway clones.
- **#1317 confirmed** — both plans that went through plan-eval approved in exactly 2 rounds with the binding rule read from the skill bodies, not from dispatch-prompt coaching (round-1 prompts carried none; the round-2 prompts quoted the evaluator's prescription because fullsend Step 3 now says to). Loop-own median 18.6M ≤ 20M. Cycle 5: 3–4 rounds; cycle 6 (by hand): 2; cycle 7 (coached): 1.
- **#1306 confirmed** (Step 2, calibration run #4).
- **#1321 / #1322 / #1323** — mass verdicts now: #1322 +145 words ✓ (≤ +200 tokens); #1321 hooks LOC +464 ✗ vs +≤150 (cage tests 5/5 · 3/3 · 3/3 unchanged ✓, `human` 0 ✓); #1323 +22 vs +≤15 (evaluator fix included). Friction/cost verdicts deferred to cycle 9 (`verdict-candidates: 1323 1322 1321`): #1321 → hook-denial lines 15 → ≤ 4, FP share ≤ 2, first cycle with all three masked hooks live from Step 0; #1322 → loop-own median ≤ 25M AND GREEN/pr-eval tool uses (cycle 8 GREEN: 90) — the rule must be live from the worktree cut; #1323 → skill-prescribed `--base` denial 1 → 0 (the `gh pr create` text-scan FPs are a separate class, backlog).

**Usage:** start five_hour=9 seven_day=33 → end five_hour=36 seven_day=38 (27 five-hour points; EST5 was 27).

**Hand-off (cycle 9 candidates):** `enforce-base-branch.py` text-scan FPs (6 lines — apply `command_mask.py`, D); #46 fullsend 1a attachment ingest hard-denied (third cycle, D); #39 checkpoint-tag pull (fourth recurrence, D); the #1322 "subject" rule under-specification + mandatory `PRE-EXISTING:` list in the PR body (A); `filter-trusted-comments.sh --json` shape doc + fullsend greenlight source column (D); #51 calibration reset (D, needs an operator-launched run #5). Cost: watch the B-row (64M) — planner 195k + GREEN 265k output are the two heaviest agents of the cycle; if cycle 9's B GREEN stays > 60 tool uses with #1322 live, the rule needs teeth (a chunk-skip when base CI is green), not prose.

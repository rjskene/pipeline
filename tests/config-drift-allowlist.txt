# Allow-list for scripts/check-config-drift.sh
# One PIPELINE_* token per line. Blank lines and `#` comments ignored.
# Each entry (or group) must explain WHY it is exempt.
# Entries ending in `_` are concat-prefix wildcards: they suppress any token
# whose name begins with that prefix (e.g. `PIPELINE_EVAL_` covers
# `PIPELINE_EVAL_CLASSIFIER`, `PIPELINE_EVAL_CONTAINER`, etc.). Entries shrink
# as the companion cleanup issue documents each var in pipeline.config.example.

# Concat-prefix bases for dynamic PIPELINE_* var construction. spawn-claude.sh
# and related scripts build var names like `PIPELINE_PATH_${X}_SKILLS_EXECUTE`
# at runtime via bash indirect expansion, so the bare prefix tokens appear in
# source. Also catches concrete vars sharing the prefix that haven't been
# documented yet (the cleanup sub-issue will peel each as it documents them).
PIPELINE_EVAL_
PIPELINE_PATH_
PIPELINE_CI_
PIPELINE_LABELS_
PIPELINE_CONTAINER_
# #721 per-model token pricing: cost-latency-report.sh builds rate var names
# like `PIPELINE_PRICE_${norm}_${bucket}` at runtime via bash indirect
# expansion, so the bare `PIPELINE_PRICE_` prefix appears in source. The
# concrete keys (PIPELINE_PRICE_CLAUDE_OPUS_4_8_{INPUT,OUTPUT,CACHE_CREATION,
# CACHE_READ}) ARE declared in pipeline.config.example and guarded by
# tests/test-tokenomics-pricing-config.sh; only the dynamic prefix is exempt.
PIPELINE_PRICE_
# #734: tests/test-init-script.sh asserts the generated config seeds a commented
# PIPELINE_PRICE_* block, using a `grep -iE '^[[:space:]]*PIPELINE_PRICE'` anchor
# whose pattern fragment ends at the bare `PIPELINE_PRICE` token (no trailing `_`,
# so the PIPELINE_PRICE_ concat-prefix above does not cover it). Not a config knob
# — a regex literal in the test. Exact-match exempt.
PIPELINE_PRICE

# Internal-state markers (script-local globals, not user-config knobs).
PIPELINE_LOGGING_SH_SOURCED
PIPELINE_BODY_OUT
PIPELINE_BASENAMES
PIPELINE_HOOK_NAMES
PIPELINE_DIFF_PLUGIN_ROOT_MODE
PIPELINE_INSTALLED_PLUGINS_FILE
PIPELINE_RPR_FILE
PIPELINE_RPR_PWD
PIPELINE_RESOLVE_MODE
# #625 dogfood local-marketplace tie-break: internal python-helper env vars
# (RPR_IPFILE/RPR_SETTINGS) plus the test-only settings-file override knob
# (PROJECT_SETTINGS_FILE), used by _resolve-plugin-root.sh + doctor.sh. Not
# user-config knobs — same class as PIPELINE_INSTALLED_PLUGINS_FILE above.
PIPELINE_RPR_IPFILE
PIPELINE_RPR_SETTINGS
PIPELINE_PROJECT_SETTINGS_FILE

# Per-spawn injected vars (orchestrator-to-subprocess contract, not user-config).
PIPELINE_ISSUE_NUMBER
PIPELINE_REPO_OVERRIDE
PIPELINE_REPO_ROOT
PIPELINE_PLUGIN_CACHE_DIR
PIPELINE_SPAWN_DRY_RUN
PIPELINE_RUNS_LOG_OVERRIDE
PIPELINE_TEST_TMPDIRS
PIPELINE_VERSION

# Test-isolation env override for check-config-drift.sh itself (points the
# lint at a fixture allowlist under mktemp -d during sub-case 4). Not a
# user-config knob; intentionally undocumented in pipeline.config.example.
PIPELINE_CONFIG_DRIFT_ALLOWLIST

# Test-isolation env override for doctor.sh's dogfood_symlink_durable check
# (#624). Points the check at a fixture installed_plugins.json under mktemp -d
# in tests/test-doctor-dogfood-symlink-durable.sh. Not a user-config knob;
# intentionally undocumented in pipeline.config.example.
PIPELINE_INSTALLED_PLUGINS_JSON

# #857/#762: PIPELINE_FRONTEND_PORT_OFFSET was removed (0 readers repo-wide) from
# pipeline.config.example and scripts/init.sh. The ONLY remaining occurrence of
# the token is the negative regression-guard in tests/test-pipeline-config-prune-762.sh,
# which asserts the var stays absent from the example/live config. That literal
# token in the test file is picked up by the live-tree scan as "referenced";
# exempt it so the deliberately-removed knob is not mis-flagged as drift.
PIPELINE_FRONTEND_PORT_OFFSET

# Fixture tokens used only inside tests/test-check-config-drift.sh's
# synthetic sub-cases. The live tree scan picks them up because they appear
# as heredoc literals in the test file itself. PIPELINE_TEST_PREFIX_ is a
# concat-prefix wildcard covering the sub-case 4b fixture vars
# (PIPELINE_TEST_PREFIX_FOO / _BAR).
PIPELINE_TEST_ORPHAN
PIPELINE_TEST_UNDOC
PIPELINE_TEST_PREFIX_
# #1132 synthetic fixture token from tests/test-check-cross-cutting-guards.sh.
# The token appears as a heredoc literal in the locked test file (tests/ dir) —
# the test injects it into a scripts/ fixture to exercise the aggregator's
# strict-fail path. The standard full-tree check-config-drift scan picks it up
# from the test file itself; this entry silences that legitimate-literal
# false-positive so standalone `bash scripts/check-config-drift.sh` and
# tests/test-config-drift-clean.sh Assertion 1 stay green on the clean tree.
# (The aggregator scripts/check-cross-cutting-guards.sh detects the scripts/
# fixture via a dedicated source-tree tripwire that assembles the token at
# runtime, so it does not depend on this allowlist entry being absent.)
PIPELINE_TEST_CROSS_CUTTING_UNDOC_1132

# Genuinely undocumented but in-use. Companion cleanup issue documents these
# in pipeline.config.example and peels them from this allowlist as each lands.
# #579 peeled PIPELINE_ANALYZE_MIN_AGE_HOURS, PIPELINE_REPO_CONSUMERS,
# PIPELINE_QUEUE_DRY_RUN, and PIPELINE_CI_FIX_CONTEXT (via the PIPELINE_CI_
# prefix collapse) — remaining entries below await follow-up doc-debt PRs.
PIPELINE_SCREENSHOT_REWRITE_ENABLED
PIPELINE_STALL_CPU_THRESHOLD
PIPELINE_STALL_SAMPLES_PER_POLL
PIPELINE_STALL_SAMPLE_INTERVAL_SEC
PIPELINE_SUBTREE_BRANCH
PIPELINE_SUBTREE_REMOTE

# #1038 doctor --fix config / detector fixtures. The live-tree scan picks these
# up as heredoc literals inside tests/test-doctor-fix-config.sh's synthetic
# fixture pipeline.config.example (PIPELINE_ALPHA / _BETA / _COMMENTED are
# fixture-only knob names that exercise append-missing / never-overwrite /
# skip-commented). Not real config knobs — exempt so the fixtures are not
# mis-flagged as drift.
PIPELINE_ALPHA
PIPELINE_BETA
PIPELINE_COMMENTED
# #1051 insert-before-set+a fixture vars (PIPELINE_GAMMA for the set-a/set+a case,
# PIPELINE_DELTA for the EOF-fallback / no-set+a case). Same exemption class.
PIPELINE_GAMMA
PIPELINE_DELTA
# PIPELINE_MOCK_WEB_EVAL_* is the per-operator, host-only mock-web-eval family
# (intentionally NOT documented in pipeline.config.example — it carries
# per-operator paths per CLAUDE.md config-conventions). It appears here both as
# the placeholder-classification glob in scripts/doctor.sh's --fix config block
# and as a fixture key in tests/test-doctor-fix-config.sh. The bare prefix is a
# concat-prefix wildcard covering PIPELINE_MOCK_WEB_EVAL_X and any sibling.
PIPELINE_MOCK_WEB_EVAL_

# #1089 split-role shared-test exemption. PIPELINE_SPLIT_ROLE_SHARED_TESTS is an
# eval-caller-set-ONLY var: evaluate-issue-pr resolves the approved plan's
# `**Shared tests (split-role):**` section and threads the paths into
# scripts/split-role-gate.sh on the invocation. Like its sibling
# PIPELINE_CI_ROLLUP_GREEN (covered by the PIPELINE_CI_ prefix above), it is
# NEVER read from pipeline.config — reading it from config would make a
# host-global exemption, defeating per-issue scoping. Intentionally undocumented
# in pipeline.config.example; exact-match exempt here.
PIPELINE_SPLIT_ROLE_SHARED_TESTS

# #1233 capability-refusal sentinel source knob. PIPELINE_CAPABILITY_REFUSAL_SOURCES
# is an eval-caller-set-ONLY var: evaluate-issue-pr's Step 11.2 resolves the
# default dogfood-only subagent log dir at the auto_merge_should_fire call site
# and threads it into scripts/check-capability-refusal.sh via
# scripts/auto-merge-gate.sh. Same shape as its sibling
# PIPELINE_SPLIT_ROLE_SHARED_TESTS above — NEVER read from pipeline.config
# (a host-global value would invite scanning the operator's real
# .claude/logs/subagents/ nondeterministically, defeating per-issue scoping).
# Intentionally undocumented in pipeline.config.example; exact-match exempt here.
PIPELINE_CAPABILITY_REFUSAL_SOURCES

# #1128 next-branch routing knobs. PIPELINE_NEXT_BRANCH / PIPELINE_NEXT_LABEL are
# defaults-in-code (#1052): COMMENTED in pipeline.config.example + the init heredoc
# with read-site ${VAR:-next} defaults at every consumer (render-status-table.sh,
# setup-worktree.sh, doctor.sh, housekeeping.md, fullsend Step 5). Commenting keeps
# test-doctor-golden-seed-set.sh green (a seeded knob would pin its value and defeat
# central default evolution), so the drift lint sees them as referenced-but-undeclared.
# Same intentionally-undocumented class as PIPELINE_SPLIT_ROLE_SHARED_TESTS above. The
# PIPELINE_NEXT_ prefix wildcard covers both *_BRANCH/*_LABEL; PIPELINE_NEXT exact-matches
# the bare-prefix token the scan extracts.
PIPELINE_NEXT
PIPELINE_NEXT_

# #1186 stage-model pins: PIPELINE_STAGE_MODEL_CLASSIFY is NOT a knob — it is the
# named follow-up in the design spec's Open items (classify is deliberately left
# INHERITING; the resolver already has the shape for it if tokenomics shows the
# cheap stage is worth pinning). The token exists in the tree only as that
# documentation prose in docs/superpowers/specs/2026-07-27-stage-model-pins-design.md,
# so the live-tree scan reports it as referenced-but-undeclared. Same
# deliberately-absent-knob class as PIPELINE_FRONTEND_PORT_OFFSET above. Exact-match
# (NOT a prefix wildcard) so the two REAL stage knobs shipped by #1186 —
# PIPELINE_STAGE_MODEL_PLAN_EVAL / PIPELINE_STAGE_MODEL_PR_EVAL — stay covered by
# the lint via their pipeline.config.example declarations. Peel this entry when the
# follow-up lands a real read-site + declaration.
PIPELINE_STAGE_MODEL_CLASSIFY

# #1275 spec-only knob. PIPELINE_TRUST_PROFILE is named in the evolve-loop design
# spec (docs/superpowers/specs/2026-09-05-harness-evolve-loop-design.md §12) as a
# FUTURE trust-profile knob (strict|lean); no read site exists anywhere in the
# tree. check-config-drift.sh scans docs/, so that prose mention reads as
# referenced-but-undeclared and reds the clean-tree suite. Same
# documented-but-not-yet-implemented class as PIPELINE_STAGE_MODEL_CLASSIFY above.
# Exact-match (NOT a prefix wildcard). Peel this entry when the trust-profile
# issue lands a real read-site + a pipeline.config.example declaration.
PIPELINE_TRUST_PROFILE

# #1272 fixture-prose-only knob. PIPELINE_SUPERPOWERS_* is a HYPOTHETICAL per-stage
# superpowers gate named only in the evolve-loop tracker's `## Hypothesis backlog`
# (item 11: "per-stage `PIPELINE_SUPERPOWERS_*` knob or trust-profile row"). That
# tracker body is mirrored VERBATIM into tests/fixtures/run-retro/tracker*.md — the
# fixtures are a byte-for-byte copy by test contract, so the prose cannot be edited
# out. check-config-drift.sh scans tests/, so the mention reads as
# referenced-but-undeclared and reds the clean-tree suite. No read site exists
# anywhere in the tree. Same spec-only class as PIPELINE_TRUST_PROFILE above, but a
# concat-prefix wildcard (the backlog names the family with a `*`, so this covers
# every PIPELINE_SUPERPOWERS_* spelling). Peel when the knob is implemented and
# documented in pipeline.config.example.
PIPELINE_SUPERPOWERS_

# #1281 test-only clock injection. PIPELINE_RETRO_NOW pins the OPEN upper bound
# of scripts/run-retro.sh's cycle window (the newest cycle has no successor
# `Cycle N+1 (<date>` header, so its bound would otherwise be `date -u` and every
# cycle-N assertion would drift with the calendar). It is set ONLY by
# tests/test-run-retro.sh on the invocation; reading it from pipeline.config
# would make a host-global clock override and silently freeze every operator's
# retro window. Same eval-caller-set-ONLY class as
# PIPELINE_SPLIT_ROLE_SHARED_TESTS above; the operator-facing seam is the
# `--now DATE` flag, documented in `run-retro.sh --help`. Intentionally
# undocumented in pipeline.config.example; exact-match exempt here.
PIPELINE_RETRO_NOW

# #1286 split-role RED window (TRANSIENT — peel when #1286's GREEN declares it).
# The `[split-role-red]` commit introduces the literal PIPELINE_HEADLESS into
# tests/test-fullsend-headless-contract.sh — tests/ is a check-config-drift scan
# dir, so the token reads as referenced-but-undeclared until the GREEN role adds
# the COMMENTED `#PIPELINE_HEADLESS="false"` declaration to
# pipeline.config.example. Without this entry the RED commit reds
# scripts/check-config-drift.sh, tests/test-config-drift-clean.sh and
# scripts/check-cross-cutting-guards.sh for reasons unrelated to the assertions
# under test. Exact-match, deliberately NOT a prefix wildcard.
# GREEN role: delete this block in the same commit that declares the knob — the
# test's A8 assertion (drift lint exits 0, token unreported) then holds on its
# own merits rather than on a suppression.
PIPELINE_HEADLESS
